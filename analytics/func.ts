import analytics from '@react-native-firebase/analytics'
import {EventType} from './event'

// // 화면 진입 시
// await analytics().logEvent('screen_view', {
//   screen_name: 'DiaryMain',
// })

// export const logEvent = (event: EventType, params: Record<string, any>) => analytics().logEvent(event, params)
